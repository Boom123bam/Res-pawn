<script>
  import { createPlaylist } from "../../modules/firebase";
</script>

<div class="page-content">
  <button
    class="cta"
    on:click={() => {
      // createPlaylist("", []);
    }}>add</button
  >
</div>
